# PlatformIO / ESP-IDF build output
.pio/
build/
sdkconfig
sdkconfig.old
sdkconfig.feather_s3
sdkconfig.devkitc_s3
managed_components/
dependencies.lock

# Built fixtures
fixtures/

# Secrets (copy secrets.h.example -> secrets.h)
src/secrets.h

# Real device credentials — keep the committed placeholders, never commit real ones.
# (If you overwrite the placeholders with real creds, do not `git add` them.)
