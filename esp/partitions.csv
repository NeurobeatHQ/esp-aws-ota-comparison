# ESP32-S3, 4 MB flash — POC OTA partition table.
#
# POC-minimal on purpose: two equal app slots + otadata, NO factory/recovery
# image (that recovery image, captive portal and A/B identity re-key are a
# separate product concern and explicitly out of scope here).
#
# Rollback requires: an `otadata` partition + two `app` slots (ota_0/ota_1).
# First esptool flash lands in ota_0 and boots as the default app (otadata is
# empty -> state UNDEFINED, so it is NOT treated as a trial image). Only an
# OTA-delivered image boots as PENDING_VERIFY and must self-test to commit.
#
# Where hardening would slot in later (kept OFF for dev units):
#   * Secure Boot v2 / Flash Encryption -> burned via eFuse, no table change.
#   * esp_secure_cert (DS peripheral)    -> the commented partition below.
#
# Name,            Type, SubType,  Offset,    Size,      Flags
nvs,               data, nvs,      0x9000,    0x6000,
otadata,           data, ota,      0xF000,    0x2000,
phy_init,          data, phy,      0x11000,   0x1000,
# esp_secure_cert, 0x3F, 0x06,     0x12000,   0x2000,   # (placeholder) DS-peripheral creds — unused in POC
ota_0,             app,  ota_0,    0x20000,   0x190000,
ota_1,             app,  ota_1,    0x1B0000,  0x190000,
storage,           data, nvs,      0x340000,  0x10000,
coredump,          data, coredump, 0x350000,  0x10000,
